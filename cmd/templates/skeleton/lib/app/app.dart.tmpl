import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../core/theme/app_theme.dart';
import 'router.dart';

/// The application shell: theme and router, nothing else.
class {% .AppClass %}App extends ConsumerWidget {
  const {% .AppClass %}App({super.key{% if .Flavors %}, required this.environment{% end %}});
{% if .Flavors %}
  /// Which flavor this build is running: development, staging, or production.
  final String environment;
{% end %}

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final router = ref.watch(routerProvider);

    return MaterialApp.router(
      title: '{% .AppTitle %}',
      debugShowCheckedModeBanner: false,
      theme: AppTheme.light,
      darkTheme: AppTheme.dark,
      routerConfig: router,
    );
  }
}
